var score_amount_id = '#score_amount_<%= @criterium_id %>_<%= @option_id %>'
$(score_amount_id + ' option').attr('selected', false);
$(score_amount_id + ' option[value="<%= @bin_id %>"]').attr('selected', true);
