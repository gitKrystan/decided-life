
<%= link_to 'New Matrix', new_matrix_path, id: 'new-matrix', class: 'btn btn-primary' %>

<% if @matrices.any? %>
<ul class="nav nav-pills nav-stacked">
  <% @matrices.each do |matrix| %>
  <li><%= link_to matrix.name, matrix %></li>
  <% end %>
</ul>
<% else %>
<h1>Add a new Decision Matrix</h1>
<% end %>
