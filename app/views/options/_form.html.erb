<%= render 'layouts/errors', object: @option %>

<%= form_for [@matrix, @option] do |f| %>
  <div class="form-group">
    <%= f.label :name, class: "control-label" %>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit id: 'save-option', class: "btn btn-success" %>
  </div>
<% end %>
